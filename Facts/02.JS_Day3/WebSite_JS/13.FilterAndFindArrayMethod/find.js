// So find is just like filter but instead of returning a array it return a single value 
//which comes first in array
const exArray=[7,53,8,10,43];
const filterArray=exArray.find((el)=>el>10);
console.log(exArray);
console.log(filterArray);